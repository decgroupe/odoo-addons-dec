Show related sales order line for service products. The service must have the
"Subcontract Service" enabled on the Purchase tab before Sale Order validation.

The "Origin Sale Item" is visible in the list view (optional) and the form view if set.
