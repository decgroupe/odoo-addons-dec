No configuration is needed. The permanent session behaviour is activated
client-side by sending the `X-Odoo-Session-Permanent: True` HTTP header on the
authentication request. This can be done in a JavaScript frontend or any API
client.
