Allow to change BoM reference used in a production order. Default
behavior in Odoo is to set the BoM reference at the creation of the
production order and not allow to change it afterwards. This module
allows to change the BoM reference on a production order, even if it is
already confirmed by unlocking it.

Note that it is only a cosmetic change as nothing will change in the
existing production order.
